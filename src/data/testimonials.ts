import type { Testimonial } from "./types";

/**
 * Add recommendations here as they arrive — the section renders
 * an elegant invitation state while this list is empty.
 */
export const testimonials: Testimonial[] = [];
